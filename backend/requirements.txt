# Runtime dependencies — Phase 1 (classical NLP, no LLMs)
pydantic>=2.6
pydantic-settings>=2.2
rank-bm25>=0.2.2
scikit-learn>=1.4
numpy>=1.26
pysbd>=0.3.4
snowballstemmer>=2.2
pymupdf>=1.24          # PDF extraction
python-docx>=1.1       # DOCX extraction
python-pptx>=0.6       # PPTX extraction (Phase 5)
openpyxl>=3.1          # XLSX extraction (Phase 5)
pdfplumber>=0.11       # table extraction (Phase 4)

# --- Storage (Phase 5) ---
sqlalchemy>=2.0        # optional SQL store backend (SQLite/Postgres)
psycopg[binary]>=3.1   # Postgres driver (for Neon and other Postgres backends)

# --- Auth ---
PyJWT>=2.8
bcrypt>=4.1
pydantic[email]>=2.6

# --- API (Phase 2) ---
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # multipart/form-data file uploads

# --- OCR (Phase 2, optional at runtime; needs the `tesseract` binary) ---
pytesseract>=0.3.10
pillow>=10.0

# --- LLM (Gemini Flash) ---
google-genai>=2.0
